begin;

insert into storage.buckets (
  id,
  name,
  public,
  file_size_limit,
  allowed_mime_types
)
values (
  'product-images',
  'product-images',
  true,
  8388608,
  array['image/jpeg', 'image/png', 'image/webp']
)
on conflict (id) do update
set file_size_limit = excluded.file_size_limit,
    allowed_mime_types = excluded.allowed_mime_types;

-- No INSERT, UPDATE, or DELETE policy is created for anon/authenticated roles.
-- Administrators upload through Supabase Studio. The public flag only allows
-- Meta to retrieve explicitly published product assets by URL.

commit;
